# Netlify config for the Vite SPA. Set VITE_API_URL in the Netlify UI.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /admin) resolve to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
